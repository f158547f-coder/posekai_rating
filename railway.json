{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "node prestart.cjs && tsx server/index.ts",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
